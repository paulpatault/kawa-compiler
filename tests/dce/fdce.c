// #include <stdio.h>
// #include <stdlib.h>

/* void meth(int n) {
    int a, b = n;
    while(a);
} */

int main( int argc, char **argv )  {
    // int a = atoi(argv[1]);
    // puts(argv[1]);
    // meth(a);
    return 0;
}
